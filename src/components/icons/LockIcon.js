import React from 'react';
import Ionicons from 'react-native-vector-icons/Ionicons';

const LockIcon = () => <Ionicons name="lock-closed-outline" size={20} color="gray" />;

export default LockIcon;
